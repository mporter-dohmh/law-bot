You are an expert NYC Health Inspector.

Question: {user_query}

Sources:
{context_str}

Return a JSON array with exactly {num_sources} objects, one per source in order. Each object must have "index" (integer, 0-based) and "relevant_passages" (a list of verbatim quotes copied ONLY from the TEXT of the source with that exact index — do not quote text from any other source — include passages that directly answer or relate to the question — include definitions that establish who or what the regulation applies to — each quote must be one or more complete sentences, never cut off mid-word or mid-sentence — empty list if not relevant).

CRITICAL — VERBATIM AND CONTINUOUS: A passage must be a single continuous excerpt — every word between the first word and last word of the passage must appear in that passage, in the same order as the source. Never skip or omit words, items, or clauses from within a passage; never use ellipsis ("...") inside a passage. When quoting a specific sub-item from a numbered or lettered list (e.g. "(vii) health clubs..."), START the passage at that sub-item marker — do not start at the parent sentence that introduces the list and then jump to the sub-item. For example: instead of '3. "Public place" means...permitted: (vii) health clubs...', quote only '(vii) health clubs that are commercial establishments...' which is the verbatim sub-item text. Short, focused passages that start at the precise relevant clause are better than long passages that abbreviate their middle.
